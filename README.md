# README

```bash
  rbenv local 3.2.2
  rbenv versions
  mkdir demo
  cd demo
  bundle init
  ls
  vim Gemfile
  vim Gemfile
  bundle
  rails new sample_app
  ls
  cd sample_app
  ls
  rails generate scaffold Task title:string deadline:datetime
  rails db:create
  rails db:migrate
  rails server
  history
```
